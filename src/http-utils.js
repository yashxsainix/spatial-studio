const path = require('path');
function sendJson(res, status, data, extraHeaders={}) { const body = JSON.stringify(data); res.writeHead(status, { 'Content-Type':'application/json; charset=utf-8', 'Content-Length':Buffer.byteLength(body), 'Cache-Control':'no-store', ...extraHeaders }); res.end(body); }
function sendText(res, status, text, type='text/plain; charset=utf-8', extraHeaders={}) { res.writeHead(status, { 'Content-Type':type, 'Content-Length':Buffer.byteLength(text), ...extraHeaders }); res.end(text); }
async function readJson(req, maxBytes=2_000_000) { return new Promise((resolve,reject)=>{ let data='',size=0,done=false; const fail=e=>{if(done)return;done=true;reject(e)}; req.on('data',c=>{size+=c.length;if(size>maxBytes){fail(new Error('Request too large')); req.destroy();return;}data+=c}); req.on('end',()=>{if(done)return;done=true;try{resolve(data?JSON.parse(data):{})}catch{reject(new Error('Invalid JSON'))}}); req.on('error',fail); }); }
function safeName(name) { const base=path.basename(String(name||'upload.bin')); return base.replace(/[^a-zA-Z0-9._()\- ]+/g,'_').slice(0,180)||'upload.bin'; }
function mimeFromFile(filename) { const ext=path.extname(filename).toLowerCase(); return ({'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'application/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.svg':'image/svg+xml','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.webp':'image/webp','.gif':'image/gif','.mp4':'video/mp4','.mov':'video/quicktime','.webm':'video/webm','.m4v':'video/x-m4v','.zip':'application/zip','.ply':'application/octet-stream','.splat':'application/octet-stream','.spz':'application/octet-stream','.sog':'application/octet-stream','.glb':'model/gltf-binary','.gltf':'model/gltf+json'})[ext]||'application/octet-stream'; }
function securityHeaders(){return {'X-Content-Type-Options':'nosniff','X-Frame-Options':'SAMEORIGIN','Referrer-Policy':'no-referrer','Permissions-Policy':'camera=(), microphone=(), geolocation=()'};}
module.exports={sendJson,sendText,readJson,safeName,mimeFromFile,securityHeaders};
